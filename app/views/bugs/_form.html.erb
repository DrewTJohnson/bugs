<%= form_with(model: bug, local: true) do |form| %>
  <% if bug.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bug.errors.count, "error") %> prohibited this bug from being saved:</h2>

      <ul>
      <% bug.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :bug_title %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author, id: :bug_author %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :bug_body %>
  </div>

  <div class="field">
    <%= form.label :priority %>
    <%= form.text_area :priority, id: :bug_priority %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_area :state, id: :bug_state %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
